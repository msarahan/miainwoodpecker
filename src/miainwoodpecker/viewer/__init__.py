"""
Napari/Qt live viewer shell.

Everything in this package requires the ``viewer`` optional dependency
group; running against the simulated microscope additionally requires
the ``device`` group (see :mod:`miainwoodpecker.viewer.app`).
"""
