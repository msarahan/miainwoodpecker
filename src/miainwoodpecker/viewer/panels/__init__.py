"""
The live viewer's panels, one module per panel.

``live.py`` assembles them; each module here builds one panel's widgets
onto the :class:`~miainwoodpecker.viewer.live.LiveInstrumentWidget` that
owns them.
"""
